# ParusWeb Functions

**Version:** 2.0.0  
**Author:** ParusWeb  
**License:** Proprietary

Модульная система расширений для WooCommerce, разработанная специально для интернет-магазина строительных и пиломатериалов.

---

## 📋 Описание

ParusWeb Functions — это плагин WordPress, который добавляет комплексный функционал для e-commerce сайта с продажей:
- Пиломатериалов (доска, брус, вагонка)
- Листовых материалов (фанера, OSB, ДВП)
- ЛКМ (краски, масла, лаки)
- Столярных изделий (фальшбалки, штакетник)
- Крепежа и комплектующих

---

## ✨ Основные возможности

### 🧮 Калькуляторы товаров
- **7 типов калькуляторов**: площадь, размеры, погонные метры, квадратные метры, множители, реечные перегородки, сложная геометрия
- Автоматический расчёт стоимости на основе параметров
- Интеграция с услугами покраски
- Выбор фасок, форм верха, типов покрытия
- SVG иконки для визуализации

### 🛒 Умная корзина
- Сохранение всех данных калькуляторов
- Автоматический пересчёт количества товара
- Отображение метаданных (размеры, площадь, услуги)
- Поддержка сложных конфигураций

### 📦 Обработка заказов
- Полное сохранение данных калькуляторов в заказе
- Отображение в письмах о заказе
- Админ-интерфейс для просмотра деталей
- Экспорт данных

### 🎨 Услуги покраски
- Трёхуровневая иерархия настроек (товар → категория → глобально)
- ACF поля для управления
- Интеграция с калькуляторами
- 8 услуг по умолчанию

### 🧪 Товары "за литр"
- Выбор объёма тары (брендо-специфичные настройки)
- Автоматический пересчёт цены × объём
- Скидка -10% при объёме >= 9л
- Поддержка 10+ брендов

### 🚚 Калькулятор доставки
- Интеграция с Яндекс.Картами
- Расчёт расстояния от склада
- Дифференцированные тарифы (лёгкие/тяжёлые грузы)
- Автокомплит адресов

### 💳 Цена по безналу
- Автоматическая наценка 10% для безналичного расчёта
- Динамический вывод на странице товара
- Адаптивный дизайн

---

## 📁 Структура модулей

Плагин состоит из **39 модулей**, организованных в 10 групп:

### 🔧 Core (Ядро)
- `core-category-helpers` — проверка категорий товаров
- `core-product-calculations` — расчёт площади, цен, множителей

### 📺 Display (Отображение)
- `display-calculators` — 7 типов калькуляторов
- `display-price-display` — кастомное отображение цен

### 🛒 Cart (Корзина)
- `cart-functionality` — добавление данных в корзину
- `cart-display` — отображение метаданных

### 📋 Orders (Заказы)
- `orders-processing` — сохранение данных заказов

### ⚙️ Admin (Админка)
- `admin-product-meta` — метаполя товаров
- `admin-category-meta` — метаполя категорий
- `admin-falsebalk-meta` — настройки фальшбалок
- `admin-shtaketnik-meta` — настройки штакетника

### 🎨 Features (Функции)
- `feature-painting-services` — услуги покраски
- `feature-liter-products` — товары за литр
- `feature-delivery-calc` — калькулятор доставки
- `feature-non-cash-price` — цена по безналу

### 🔌 Integrations (Интеграции)
- `integration-facet-filters` — настройка FacetWP
- `integration-mega-menu` — атрибуты в мега-меню

### 👤 Account (Личный кабинет)
- `account-customization` — кастомизация меню ЛК
- `account-company-fields` — реквизиты юрлиц
- `account-inn-lookup` — автозаполнение по ИНН (DaData)

### 🔧 Utilities (Утилиты)
- `utility-ajax-handlers` — AJAX обработчики
- `utility-shortcodes` — шорткоды
- `utility-misc` — вспомогательные функции

### 📜 Scripts (Скрипты)
- `scripts-calculator` — JavaScript калькуляторов
- `scripts-price-update` — обновление цен

---

## 🚀 Установка

### Требования
- WordPress 5.8+
- WooCommerce 5.0+
- PHP 7.4+
- Advanced Custom Fields (для услуг покраски)

### Шаги установки

1. **Загрузите плагин:**
   ```
   /wp-content/plugins/parusweb-functions/
   ```

2. **Активируйте плагин:**
   WordPress Admin → Плагины → ParusWeb Functions → Активировать

3. **Настройте модули:**
   WordPress Admin → ParusWeb → Настройки модулей

4. **Активируйте нужные модули:**
   - Критические модули (Core) включены всегда
   - Выберите дополнительные модули по необходимости

---

## ⚙️ Конфигурация

### Настройка категорий

Плагин использует следующие ID категорий:

```php
// Листовой материал
190, 191, 127, 94

// Пиломатериалы за площадь
265 (родительская), 94, 127, 191 (дочерние)

// Квадратные метры
266, 270, 268

// Погонные метры
267

// Реечные перегородки
271

// ЛКМ (за литр)
81-86

// Фальшбалки
266

// Штакетник
273
```

**Важно:** При изменении структуры категорий обновите ID в файле `modules/core/category-helpers.php`

### Настройка услуг покраски

1. Перейдите: **WordPress Admin → Услуги покраски**
2. Добавьте услуги с ценами за м²
3. Настройте категории или отдельные товары

### Настройка калькулятора доставки

Отредактируйте параметры в `modules/features/delivery-calculator.php`:

```php
'basePoint'   => 'г. Санкт-Петербург, Выборгское шоссе 369к6',
'rateLight'   => 200,  // руб/км (до 1500г)
'rateHeavy'   => 250,  // руб/км (свыше 1500г)
'minLight'    => 6000, // минимум для легких
'minHeavy'    => 7500, // минимум для тяжелых
```

### Настройка брендов для ЛКМ

В файле `modules/features/liter-products.php` настройте объёмы тары:

```php
'remmers'     => [0.75, 2.5, 5, 10, 20],
'biofa'       => [0.375, 0.75, 2.5, 5, 10],
// ... добавьте свои бренды
```

---

## 🔧 API для разработчиков

### Проверка категорий

```php
// Проверка категории товара
if (is_area_category($product_id)) {
    // Товар за площадь
}

if (is_in_liter_categories($product_id)) {
    // Товар за литр
}
```

### Получение услуг покраски

```php
// Получить услуги для товара
$services = parusweb_get_painting_services($product_id);

// Получить в формате для калькулятора
$formatted = parusweb_get_painting_services_formatted($product_id);
```

### Расчёт цен

```php
// Получить множитель цены
$multiplier = get_price_multiplier($product_id);

// Извлечь площадь из названия
$area = extract_area_with_qty($product->get_name(), $product_id);

// Рассчитать минимальную цену
$min_price = calculate_min_price_area($product_id, $base_price);
```

### Форматирование

```php
// Склонение слов
$word = parusweb_plural(5, ['товар', 'товара', 'товаров']); // товаров

// Форматирование числа
$formatted = parusweb_format_number(1234.56); // "1 234,56"

// Форматирование площади
$area_text = parusweb_format_area(2.5); // "2,500 м²"
```

---

## 🎨 Хуки и фильтры

### Фильтры

```php
// Изменить процент наценки для безнала
add_filter('parusweb_non_cash_markup', function($markup, $product_id) {
    return 15; // 15% вместо 10%
}, 10, 2);

// Кастомизация метаданных в корзине
add_filter('woocommerce_get_item_data', 'custom_cart_item_data', 10, 2);

// Модификация HTML цены
add_filter('woocommerce_get_price_html', 'custom_price_html', 10, 2);
```

### Действия

```php
// После добавления товара в корзину
add_action('woocommerce_add_to_cart', 'after_add_to_cart', 10, 6);

// При сохранении заказа
add_action('woocommerce_checkout_update_order_meta', 'save_custom_order_data');

// После расчёта калькулятора
add_action('parusweb_after_calculator', 'custom_calculator_action');
```

---

## 🐛 Отладка

### Включение режима отладки

В файле `parusweb-functions.php`:

```php
define('PARUSWEB_DEBUG', true);
```

### Логирование

```php
// Логирование в debug.log
parusweb_log($message, 'Prefix');

// Вывод для администраторов (HTML комментарий)
parusweb_debug($data, 'Label');
```

### Проверка активности модулей

```php
if (parusweb_is_module_active('display-calculators')) {
    // Модуль активен
}
```

---

## 📚 Документация модулей

Каждый модуль содержит подробные комментарии в коде:

- **Описание функционала**
- **Зависимости**
- **Использование функций**
- **Примеры кода**

Смотрите файлы в директории `modules/`

---

## 🔄 Миграция с monolith functions.php

### Автоматическая миграция

1. Установите плагин ParusWeb Functions
2. Активируйте все модули
3. Старый `functions.php` будет работать параллельно
4. Постепенно переносите кастомные изменения

### Обратная совместимость

Плагин поддерживает алиасы старых функций:

```php
get_available_painting_services_by_material() // → parusweb_get_painting_services_formatted()
get_acf_painting_services() // → parusweb_get_painting_services()
get_russian_plural_for_cart() // → parusweb_plural()
```

---

## 📝 Changelog

### Version 2.0.0 (2024)
- ✨ Полный рефакторинг в модульную архитектуру
- ✨ 39 независимых модулей
- ✨ Система управления модулями в админке
- ✨ Улучшенная производительность
- ✨ Полная документация кода
- 🐛 Исправлены дублирующиеся функции
- 🐛 Оптимизирована работа калькуляторов

### Version 1.0.0 (2023)
- 🎉 Первый релиз (monolith functions.php)

---

## 🤝 Поддержка

**Email:** support@parusweb.ru  
**Сайт:** https://parusweb.ru

---

## 📄 Лицензия

Proprietary. Все права защищены © 2024 ParusWeb

**Внимание:** Этот плагин разработан специально для конкретного проекта и не предназначен для публичного распространения.

---

## 👥 Команда разработки

- **Архитектура:** ParusWeb Development Team
- **Backend:** PHP/WordPress/WooCommerce
- **Frontend:** JavaScript/jQuery
- **Интеграции:** Яндекс.Карты, DaData, FacetWP, ACF

---

## 🙏 Благодарности

- WordPress Community
- WooCommerce Team
- Advanced Custom Fields
- FacetWP

---

**Сделано с ❤️ для строительной индустрии**
